# Prompt Library (Expanded VSCode Implementation)

These prompts assume you have:
- the lab running
- Expanded VSCode implementation ingested
- `/memory/query` available

Workflow:
1) Retrieve context from memory
2) Ask your IDE assistant to review a target file using retrieved references
3) Implement minimal fixes and validate the lab still works
